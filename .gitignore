node_modules/
dist/
.DS_Store
*.zip

# Maintainer-only (store assets, build scripts, launch docs, QA)
private/

# CWS publish secrets (env-var fallback used by scripts/cws-api.mjs)
.secrets.local.json

# E2E test build (regenerated by scripts/build-e2e.sh)
e2e-build/

# E2E artifact dumps (screenshots, diag rings) — keep the directory, not the files
tests/e2e/artifacts/*
!tests/e2e/artifacts/.gitkeep
